<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register Laptop</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Georgia', serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            background-image: url('https://cdn.pixabay.com/photo/2021/06/19/20/39/laptop-6349424_1280.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
        }

        .container {
            max-width: 600px;
            margin: 50px auto;
            background-color: rgba(255, 255, 255, 0.8); /* Slight transparency to let background show through */
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #4CAF50;
            font-size: 30px;
            font-family: 'Times New Roman', Times, serif;
            margin-bottom: 20px;
        }

        .message {
            text-align: center;
            margin-bottom: 20px;
            font-size: 16px;
            font-weight: bold;
            color: #d9534f; /* For error */
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            font-size: 18px;
            font-weight: bold;
            color: #333;
        }

        /* Stylish input box */
        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 14px;
            font-size: 16px;
            border: 2px solid #ddd; /* Soft border for subtle design */
            border-radius: 8px;
            background-color: #f0f0f0;
            box-sizing: border-box;
            transition: all 0.3s ease-in-out;
            font-family: 'Arial', sans-serif;
        }

        /* Focus effect for input fields */
        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            border-color: #4CAF50;
            background-color: #fff;
            box-shadow: 0 0 8px rgba(0, 128, 0, 0.6); /* Green glow */
            outline: none;
        }

        /* Subtle animations on hover */
        .form-group input:hover,
        .form-group select:hover,
        .form-group textarea:hover {
            background-color: #e7f5e7; /* Light green background on hover */
        }

        .form-group textarea {
            resize: vertical;
            min-height: 120px;
        }

        .btn {
            width: 100%;
            padding: 14px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 18px;
            cursor: pointer;
            margin-top: 20px;
            text-align: center;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #45a049;
        }

        .actions {
            margin-top: 20px;
            text-align: center;
        }

        .actions a {
            display: inline-block;
            padding: 12px 25px;
            background-color: #2196F3;
            color: white;
            border-radius: 6px;
            text-decoration: none;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

        .actions a:hover {
            background-color: #1976D2;
        }

        .actions button {
            padding: 12px 25px;
            background-color: #009688;
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .actions button:hover {
            background-color: #00796b;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Register Your Laptop</h1>

        <!-- Dynamic Message Section -->
        <% if (message) { %>
            <div class="message"><%= message %></div>
        <% } %>

        <!-- Laptop Registration Form -->
        <form action="/register-laptop" method="POST">
            <div class="form-group">
                <label for="serialNumber">Serial Number:</label>
                <input type="text" id="serialNumber" name="serialNumber" required>
            </div>

            <div class="form-group">
                <label for="model">Model:</label>
                <input type="text" id="model" name="model" required>
            </div>

            <div class="form-group">
                <label for="brand">Brand:</label>
                <input type="text" id="brand" name="brand" required>
            </div>

            <div class="form-group">
                <label for="warranty">Warranty (in months):</label>
                <input type="number" id="warranty" name="warranty" required>
            </div>

            <div class="form-group">
                <label for="operatingSystem">Operating System:</label>
                <input type="text" id="operatingSystem" name="operatingSystem" required>
            </div>

            <div class="form-group">
                <label for="memory">Memory (RAM):</label>
                <input type="text" id="memory" name="memory" required>
            </div>

            <div class="form-group">
                <label for="storage">Storage:</label>
                <input type="text" id="storage" name="storage" required>
            </div>

            <div class="form-group">
                <label for="comments">Additional Details (e.g., color, display size):</label>
                <textarea id="comments" name="comments" placeholder="Enter additional details"></textarea>
            </div>

            <button type="submit" class="btn">Register Laptop</button>
        </form>

        <!-- Navigation Links -->
        <div class="actions">
            <a href="/dashboard" class="btn">Go to Dashboard</a>
            <a href="/login-signup" class="btn">Back to Login</a>
        </div>

        <!-- Button to navigate to Laptop Store Map -->
        <div class="actions">
            <button onclick="window.location.href='/LaptopStoreMap/index.html';">Go to Store Map</button>
        </div>
    </div>
</body>
</html>
